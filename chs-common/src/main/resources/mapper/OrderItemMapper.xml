<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kitten.chs.common.domain.mapper.OrderItemMapper">

    <select id="selectByOrderIds" resultType="com.kitten.chs.common.domain.dataObject.OrderItemDO">
        SELECT id, order_id, food_id, food_name, food_price, quantity, subtotal, create_time
        FROM t_order_item
        WHERE order_id IN
        <foreach collection="orderIds" item="orderId" open="(" separator="," close=")">
            #{orderId}
        </foreach>
        ORDER BY order_id, create_time
    </select>

</mapper>
